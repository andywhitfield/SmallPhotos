@model SmallPhotos.Web.Model.Home.IndexViewModel

<aside>
    <nav>
        <ul>
            <li><a href="/">Nav link 1</a></li>
            <li><a href="/">Nav link 2</a></li>
        </ul>
    </nav>
</aside>

<article>
    <p>
    @if (Model.Photos.Any()) {
    <ul>
        @foreach(var photo in Model.Photos) {
            <li><img src="/photo/@(photo.PhotoId)/@(photo.Filename)?size=@(Model.ThumbnailSize)" width="@(photo.Size.Width)" height="@(photo.Size.Height)" /></li>
        }
    </ul>
    } else {
        <text>You have no photos. Add a collection to your library in your <a href="/profile">profile</a>.</text>
    }
    </p>
</article>