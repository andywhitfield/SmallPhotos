@model SmallPhotos.Web.Model.Gallery.IndexViewModel

@section Scripts {
<script type="text/javascript">
    $(function() {
        $(window).on('resize', fullSizeImage);
        fullSizeImage();
    });
</script>
}


<style>

.pagination {
  margin-top: 10px;
  display: inline-block;
}

.pagination a {
  color: black;
  float: left;
  padding: 8px 16px;
  text-decoration: none;
  transition: background-color .3s;
}

.pagination a.active {
  background-color: #6be1ff;
  color: white;
}

.pagination a:hover:not(.active) {
    background-color: #f86bff;
}

</style>

<aside>
    <nav>
        <ul>
            <li><a href="/">Nav link 1</a></li>
            <li><a href="/">Nav link 2</a></li>
        </ul>
    </nav>
</aside>

<article>
    <div>Taken: @Model.Photo.DateTaken</div>

    <div class="pagination">
        <a href="/?photoId=@(Model.Photo.PhotoId)#@(Model.Photo.PhotoId).@(Model.Photo.Filename)">&lsaquo; Thumbnails</a>
        @if (Model.PreviousPhoto != null) {
        <a href="/gallery/@(Model.PreviousPhoto.PhotoId)/@(Model.PreviousPhoto.Filename)">&laquo; Prev (#@(Model.PreviousPhotoNumber) of @(Model.PhotoCount))</a>
        }
        @if (Model.NextPhoto != null) {
        <a href="/gallery/@(Model.NextPhoto.PhotoId)/@(Model.NextPhoto.Filename)">Next  (#@(Model.NextPhotoNumber) of @(Model.PhotoCount)) &raquo;</a>
        }
    </div>

    <p>
        <img id="fullimg" src="/photo/@(Model.Photo.PhotoId)/@(Model.Photo.Filename)" />
    </p>

    <div class="pagination">
        <a href="/?photoId=@(Model.Photo.PhotoId)#@(Model.Photo.PhotoId).@(Model.Photo.Filename)">&lsaquo; Thumbnails</a>
        @if (Model.PreviousPhoto != null) {
        <a href="/gallery/@(Model.PreviousPhoto.PhotoId)/@(Model.PreviousPhoto.Filename)">&laquo; Prev (#@(Model.PreviousPhotoNumber) of @(Model.PhotoCount))</a>
        }
        @if (Model.NextPhoto != null) {
        <a href="/gallery/@(Model.NextPhoto.PhotoId)/@(Model.NextPhoto.Filename)">Next  (#@(Model.NextPhotoNumber) of @(Model.PhotoCount)) &raquo;</a>
        }
    </div>

</article>